import type * as Schema from './schema';

export interface Objects {
  Account: Schema.Account;
  AccountProvider: Schema.AccountProvider;
  User: Schema.User;
  UserAccessLog: Schema.UserAccessLog;
}
